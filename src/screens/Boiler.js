import React from "react";
import { View, StyleSheet, Text } from "react-native";

const Boiler = () => {
  return <Text>Boiler</Text>;
};

const styles = StyleSheet.create({});

export default Boiler;
